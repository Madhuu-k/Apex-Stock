apiVersion: v1
kind: Service
metadata:
  name: {{ include "apex-stock.fullname" . }}
  labels:
    {{- include "apex-stock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apex-stock.selectorLabels" . | nindent 4 }}
